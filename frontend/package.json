{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "node .output/server/index.mjs",
    "prod": "npm run generate && npm run build && npm run preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.13",
    "vite-plugin-vuetify": "^1.0.0",
    "vuetify": "^3.0.0"
  },
  "dependencies": {
    "@mdi/font": "^7.0.96",
    "sass": "^1.56.0",
    "sass-loader": "^13.1.0",
    "v3-infinite-loading": "^1.2.1"
  }
}
