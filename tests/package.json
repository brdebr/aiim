{
  "name": "aiim-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test page",
    "auth": "npm run test:auth",
    "report": "playwright show-report",
    "test:report": "playwright test page && npm run report",
    "test:auth": "playwright test auth --project=\"Google Chrome\"",
    "test:update": "playwright test page --update-snapshots",
    "test:debug": "playwright test page --headed",
    "test:mobile": "playwright test page --project=\"Samsung Galaxy Note Lite 10\"",
    "test:desktop": "playwright test page --project=\"Google Chrome\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.28.1"
  }
}
